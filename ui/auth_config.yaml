# Streamlit Authentication Configuration
#
# This file controls access to the trading dashboard.
#
# IMPORTANT: Change the default password before using in production!
#
# To generate a new password hash:
#   import streamlit_authenticator as stauth
#   hashed = stauth.Hasher(['your_password']).generate()
#   print(hashed[0])

credentials:
  usernames:
    admin:
      name: Administrator
      # Default password: admin123
      # Hash generated for streamlit-authenticator 0.2.3
      password: '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5ztJ.7E6z6KFu'

cookie:
  expiry_days: 30
  key: trading_system_secret_key_change_this
  name: trading_system_auth_cookie

preauthorized:
  emails: []
